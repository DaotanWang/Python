numRows = 3 

["" for _ in range(numRows)]
# ["","",""]

# - range(numRows) 得到 0 到 numRows-1 这numRows个数
# - 对每个数字,表达式 ""生成一个空字符串
# - 由于并不需要使用循环变量,所以使用 _ 作为变量名
# - 最终得到包含 numRows 个空字符串的列表

#_用于不需要循环的变量时，类似于占位符一样的功能。
